name: Add mask
on: push
jobs:
    log:
        runs-on: ubuntu-latest
        env:
            PASSWORD: SuperSecretValue
        steps:
            - run: echo "::add-bask::${PASSWORD}"
            - run: echo "${PASSWORD}"
